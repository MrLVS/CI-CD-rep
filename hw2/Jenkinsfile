pipeline {
    agent {
        label 'docker'
    }

    environment {
        IMAGE_REPO = 'mrlvs/java'
        TAG = "1.0.0"
    }

    stages {
        stage("Build") {
            steps {
                sh '''
                printenv
                docker pull $IMAGE_REPO:$TAG
                docker build -t $IMAGE_REPO:$TAG-$JOB_NAME ./
                '''

               
            }
        }
    }
       
}